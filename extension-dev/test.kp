// cognite_kuiper:inputs=input,context,foo,bar
{
    "test": 123,
    "hi": "foo".concat("bar"),
    if "foo" == "bar" {
        3
    } else {
        4 + round(123)
    }: "test",
    "foo": input.foo.round()
}