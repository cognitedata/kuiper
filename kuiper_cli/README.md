# Kuiper CLI

This pacakage contains the Kuiper CLI. See the [main README](./README.md#using-the-cli) for a quick guide to the CLI,
or run `kuiper --help` to show the help page.

To compile and install the CLI from source, run

``` commandline
cargo install --path .
```

from this directory. The command `kuiper` should now be available.
